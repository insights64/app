{
  "buildCommand": "elm make src/Main.elm --optimize --output=public/elm.js",
  "outputDirectory": "public",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
